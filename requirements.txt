# Neuro SAN
neuro-san==0.6.76
# Pin leaf-common into a narrow working window:
#  - 1.3.0+ dropped leaf_common.session, which leaf-server-common 0.1.25 still
#    imports (ModuleNotFoundError: No module named 'leaf_common.session').
#  - 1.2.49+ call asyncio.eager_task_factory in AsyncioExecutor, which only
#    exists on Python 3.12+. This venv is 3.11, so it breaks
#    PeriodicEventInitiator and TempNetworkStorageUpdater at runtime.
# 1.2.45-1.2.48 have session and avoid eager_task_factory. Pinned exact to
# 1.2.47 to match the known-good fleet config on the other Go2.
leaf-common==1.2.47

nsflow==0.5.20
langchain-google-genai

# To use a .env file for environment variables
python-dotenv==1.0.1

# For asynchronous file operations
aiofiles>=24.1.0

# Web app dependencies for apps/conscious_assistant/interface_flask.py
flask>=2.3,<3.0
flask-socketio>=5.3,<6.0
simple-websocket>=0.10,<2.0
openai>=1.3,<2.0
pyttsx3>=2.90 ; sys_platform == "darwin"
piper-tts>=1.2 ; sys_platform == "linux"

# Vision dependencies
numpy>=1.24
ultralytics>=8.4.7
deepface>=0.0.97
tf-keras>=2.20.1
opencv-python>=4.13.0.90

# Navigation dependencies (nav_core)
# RealSense publishes pip wheels for some Linux platforms, but not for Jetson
# aarch64. On Jetson, install librealsense2-utils/librealsense2-dev with apt
# and build the Python bindings for the active venv; see README.md.
pyrealsense2>=2.50 ; sys_platform == "linux" and platform_machine != "aarch64" and platform_machine != "arm64"
scipy>=1.10
